---
# Module-level Hiera data. Only used for lookup_options - parameter defaults live in the
# class signature.
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "common"
    path: "common.yaml"
