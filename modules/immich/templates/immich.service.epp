<%- |
  String $compose_command,
  String $compose_file,
  String $install_dir,
  String $data_dir,
  Array[String] $external_libraries,
| -%>
# Managed by Puppet. Local changes will be overwritten.
[Unit]
Description=Immich photo server (podman-compose)
Documentation=https://immich.app
Wants=network-online.target
After=network-online.target
# If the data dir or an external library is a separate mount, don't start until it is there
RequiresMountsFor=<%= ([$data_dir] + $external_libraries).join(' ') %>

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=<%= $install_dir %>
ExecStart=<%= $compose_command %> -f <%= $compose_file %> up -d --remove-orphans
ExecStop=<%= $compose_command %> -f <%= $compose_file %> down
# First start pulls several GB of images
TimeoutStartSec=900
TimeoutStopSec=120

[Install]
WantedBy=multi-user.target
