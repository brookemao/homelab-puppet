# /etc/fail2ban/filter.d/immich.conf
# Managed by OpenVox - manual edits will be overwritten
#
# Matches Immich failed login attempts:
#   "Failed login attempt for user <email> from ip address <ip>"
[Definition]
failregex = ^.*Failed login attempt for user \S+ from ip address <HOST>\s*$
ignoreregex =
